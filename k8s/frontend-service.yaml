apiVersion: v1
kind: Service
metadata:
  name: citron-frontend
spec:
  type: NodePort
  selector:
    app: citron-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080

